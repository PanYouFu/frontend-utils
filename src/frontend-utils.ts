/**
 *
 * 前端公共方法库
 *
 */

import formatBankNo from './formatBankNo';
import formatDate from './formatDate';

const frontendUtils = {
  formatBankNo,
  formatDate,
};
export default frontendUtils;
export { formatBankNo, formatDate };
